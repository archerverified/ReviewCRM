PROMPT 4: App Configuration & Layout

EXECUTION INSTRUCTIONS:
Prompt and absorb the following agents and skills commands from the workspace before executing:
.claude\agents\expert-nextjs-developer.md
.claude\agents\code-reviewer.md
.claude\agents\supabase-schema-architect.md
.claude\agents\javascript-pro.md
.claude\agents\database-architect.md
.claude\agents\api-security-audit.md
.claude\agents\ui-ux-designer.md
.claude\agents\test-engineer.md
.claude\agents\performance-engineer.md
.claude\skills\ux-researcher-designer
.claude\skills\frontend-design
.claude\skills\webapp-testing
.claude\skills\senior-backend
.claude\skills\senior-frontend
.claude\skills\react-best-practices
.claude\skills\it-operations

Target Environment: VSCode with Opus 4.5 in Plan Mode
Dependencies Required: sonner (already installed from Prompt 3)
Execution Order: Run after Prompt 3 completion
Critical Success Factors:
- All 4 files must be generated completely
- Environment variables configured with actual credentials
- Clay design system applied consistently
- Root layout with navigation functional
- Dashboard page with full CRUD operations
- No placeholder code or TODOs

SUPABASE CREDENTIALS (Pre-configured):
Project URL: https://cvchmlaouwwqwurckssw.supabase.co
Anon Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImN2Y2htbGFvdXd3cXd1cmNrc3N3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg2NjgzMTIsImV4cCI6MjA4NDI0NDMxMn0.emc0ySzXBFu5Hcj_ewM_MNA3SDUhWW-KpuQRho2naaU
Service Role Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImN2Y2htbGFvdXd3cXd1cmNrc3N3Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2ODY2ODMxMiwiZXhwIjoyMDg0MjQ0MzEyfQ.SjtxVB-6s2wu9PJ54-w8goyFzWlBMejLpB0YcUhwg7I

FIRECRAWL CREDENTIALS (Pre-configured):
API Key: fc-6588659a89ba4128b0306c393f150f3a

---

THE PROMPT:

You are a senior Next.js engineer setting up app configuration, root layout, and dashboard page for ReviewCRM.

BUSINESS CONTEXT:
- Single-user CRM for 2ndimpression.co reputation management business
- 19-stage pipeline tracking (lead_identified → completed)
- AI-powered message personalization via Firecrawl
- Plusvibe.ai CSV export workflow
- Clay design system (cream background, black text, blue accents)

DESIGN SYSTEM SPECS (Clay):
- Background: bg-[#F8F6F3] (cream)
- Primary text: text-gray-900
- Secondary text: text-gray-600
- Borders: border-black/10
- Accent: text-blue-600, bg-blue-600
- Rounded corners: rounded-xl (buttons/cards), rounded-lg (inputs)
- Shadows: shadow-sm on cards, shadow-lg on modals
- Hover states: hover:bg-black/5 with transition-colors
- Spacing: Consistent 4px grid (p-4, gap-4, space-y-4)
- Typography: Inter font, font-medium for labels, font-semibold for headings

TASK: Create 4 production-ready files.

---

FILE 1: .env.local

Create environment variables file with actual credentials:

REQUIREMENTS:
- Supabase configuration (3 keys)
- Firecrawl API key
- App URL for local development
- Comments explaining each variable
- Production-ready format

IMPLEMENTATION:
```bash
# Supabase Configuration
# Database and authentication for ReviewCRM
NEXT_PUBLIC_SUPABASE_URL=https://cvchmlaouwwqwurckssw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImN2Y2htbGFvdXd3cXd1cmNrc3N3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg2NjgzMTIsImV4cCI6MjA4NDI0NDMxMn0.emc0ySzXBFu5Hcj_ewM_MNA3SDUhWW-KpuQRho2naaU
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImN2Y2htbGFvdXd3cXd1cmNrc3N3Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2ODY2ODMxMiwiZXhwIjoyMDg0MjQ0MzEyfQ.SjtxVB-6s2wu9PJ54-w8goyFzWlBMejLpB0YcUhwg7I

# Firecrawl API (AI message generation)
# Temporary placeholder for Claude API
FIRECRAWL_API_KEY=fc-6588659a89ba4128b0306c393f150f3a

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Future integrations
# RESEND_API_KEY=re_your_key_here
# STRIPE_SECRET_KEY=sk_test_your_key_here
```

---

FILE 2: src/app/layout.tsx

Create root layout component with navigation:

REQUIREMENTS:
- Next.js 14 App Router layout
- Metadata: title, description, favicon
- Inter font from next/font/google
- Body styling: bg-[#F8F6F3] text-gray-900 antialiased
- Fixed navigation header (bg-white, border-b border-black/10)
- Navigation links: Businesses, Pipeline, Campaigns, Metrics
- Active link styling (text-blue-600, underline)
- User menu placeholder (2ndimpression.co branding, initials: AW)
- Main content area: max-w-7xl mx-auto px-4 py-8
- Toaster component from sonner (bottom-right, richColors)
- Proper TypeScript: RootLayout type

NAVIGATION STRUCTURE:
- Logo: "ReviewCRM" (left side)
- Links: Businesses (/), Pipeline (/pipeline), Campaigns (/campaigns), Metrics (/metrics)
- User section: Company name + avatar (right side)

ACTIVE LINK INDICATOR:
- Underline animation on hover
- Scale animation on hover (group-hover:scale-x-100)

IMPLEMENTATION:
```typescript
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Toaster } from 'sonner';
import Link from 'next/link';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'ReviewCRM | Reputation Management Pipeline',
  description: 'Manage your reputation management leads, campaigns, and sales pipeline for 2ndimpression.co',
  icons: {
    icon: '/favicon.ico',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-[#F8F6F3] text-gray-900 antialiased`}>
        <header className="fixed top-0 left-0 right-0 z-40 bg-white border-b border-black/10">
          <nav className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
            <Link href="/" className="text-xl font-bold text-gray-900 hover:text-blue-600 transition-colors">
              ReviewCRM
            </Link>

            <div className="flex items-center gap-6">
              <NavLink href="/" label="Businesses" />
              <NavLink href="/pipeline" label="Pipeline" />
              <NavLink href="/campaigns" label="Campaigns" />
              <NavLink href="/metrics" label="Metrics" />
            </div>

            <div className="flex items-center gap-3">
              <span className="text-sm text-gray-600">2ndimpression.co</span>
              <div className="w-8 h-8 rounded-full bg-blue-600 text-white flex items-center justify-center font-medium text-sm">
                AW
              </div>
            </div>
          </nav>
        </header>

        <main className="pt-16 min-h-screen">
          <div className="max-w-7xl mx-auto px-4 py-8">
            {children}
          </div>
        </main>

        <Toaster position="bottom-right" richColors />
      </body>
    </html>
  );
}

function NavLink({ href, label }: { href: string; label: string }) {
  return (
    <Link
      href={href}
      className="text-sm font-medium text-gray-600 hover:text-blue-600 transition-colors relative group"
    >
      {label}
      <span className="absolute -bottom-4 left-0 right-0 h-0.5 bg-blue-600 scale-x-0 group-hover:scale-x-100 transition-transform" />
    </Link>
  );
}
```

---

FILE 3: src/app/globals.css

Create global styles with Tailwind directives:

REQUIREMENTS:
- Tailwind base, components, utilities
- Custom base layer styles
- Border color inheritance
- Clay background color
- Text balance utility

IMPLEMENTATION:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * {
    @apply border-border;
  }
  
  body {
    @apply bg-[#F8F6F3] text-gray-900;
  }
}

@layer utilities {
  .text-balance {
    text-wrap: balance;
  }
}
```

---

FILE 4: src/app/page.tsx

Create dashboard page with full CRUD operations:

REQUIREMENTS:
- Client component ('use client')
- State management: businesses, filteredBusinesses, selectedIds, filters, showImportModal, loading
- Load businesses on mount (useEffect)
- Client-side filtering based on FilterState
- FilterBar component integration
- ImportModal component integration
- ExportButton component integration
- Bulk actions toolbar (visible when selectedIds.size > 0):
  * Move to stage dropdown (all 19 stages)
  * Update email status dropdown
  * Export selected button
  * Clear selection button
- Business table with:
  * Checkbox column for selection
  * Columns: Business, City, Reviews, Pricing, Project Value, Stage, Email Status
  * Color-coded pricing tier badges (bronze=yellow-100, silver=gray-100, gold=amber-100)
  * Currency formatted project values
  * Color-coded pipeline stages
  * Hover states on rows
- Empty state: "No businesses found. Import a CSV to get started."
- Loading state: centered spinner
- Header section: title, count, import button
- Proper TypeScript typing throughout

STATE INTERFACES:
- businesses: Business[]
- filteredBusinesses: Business[]
- selectedIds: Set<string>
- filters: FilterState
- showImportModal: boolean
- loading: boolean

FILTERING LOGIC:
- Search: OR query on business_name, email, city (case-insensitive)
- Stages: IN query if array not empty
- Email statuses: IN query if array not empty
- Pricing tiers: IN query if array not empty

BULK ACTIONS:
- bulkUpdateStage: Update pipeline_stage for selectedIds
- bulkUpdateEmailStatus: Update email_status for selectedIds
- Both: Update local state + clear selection + show success toast

SELECTION LOGIC:
- toggleSelection: Add/remove from Set
- toggleSelectAll: Select all filtered OR clear all
- Checkbox in header reflects: all selected AND at least one business

TABLE STYLING:
- Pricing tier badges:
  * bronze: bg-yellow-100 text-yellow-800
  * silver: bg-gray-100 text-gray-800
  * gold: bg-amber-100 text-amber-800
- Stage colors: Use color from PIPELINE_STAGES config
- Hover: hover:bg-gray-50 on rows

IMPLEMENTATION:
```typescript
'use client';

import { useState, useEffect } from 'react';
import { Button } from '@/components/ui/Button';
import { FilterBar } from '@/components/FilterBar';
import { ImportModal } from '@/components/ImportModal';
import { ExportButton } from '@/components/ExportButton';
import { businessQueries } from '@/lib/supabase';
import type { Business, FilterState, PipelineStage, EmailStatus } from '@/types';
import { PIPELINE_STAGES } from '@/types';
import { toast } from 'sonner';

const EMPTY_FILTERS: FilterState = {
  search: '',
  stages: [],
  emailStatuses: [],
  tags: [],
  pricingTiers: [],
  cities: [],
  campaigns: []
};

export default function DashboardPage() {
  const [businesses, setBusinesses] = useState<Business[]>([]);
  const [filteredBusinesses, setFilteredBusinesses] = useState<Business[]>([]);
  const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set());
  const [filters, setFilters] = useState<FilterState>(EMPTY_FILTERS);
  const [showImportModal, setShowImportModal] = useState(false);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadBusinesses();
  }, []);

  useEffect(() => {
    applyFilters();
  }, [businesses, filters]);

  const loadBusinesses = async () => {
    try {
      setLoading(true);
      const data = await businessQueries.getAll();
      setBusinesses(data);
    } catch (error: any) {
      toast.error(`Failed to load businesses: ${error.message}`);
    } finally {
      setLoading(false);
    }
  };

  const applyFilters = () => {
    let filtered = [...businesses];

    if (filters.search) {
      const search = filters.search.toLowerCase();
      filtered = filtered.filter(b =>
        b.business_name.toLowerCase().includes(search) ||
        b.email?.toLowerCase().includes(search) ||
        b.city?.toLowerCase().includes(search)
      );
    }

    if (filters.stages.length > 0) {
      filtered = filtered.filter(b => filters.stages.includes(b.pipeline_stage));
    }

    if (filters.emailStatuses.length > 0) {
      filtered = filtered.filter(b => filters.emailStatuses.includes(b.email_status));
    }

    if (filters.pricingTiers.length > 0) {
      filtered = filtered.filter(b => b.pricing_tier && filters.pricingTiers.includes(b.pricing_tier));
    }

    setFilteredBusinesses(filtered);
  };

  const handleImport = async (newBusinesses: Partial<Business>[]) => {
    try {
      const imported = await businessQueries.bulkCreate(newBusinesses);
      setBusinesses([...imported, ...businesses]);
      toast.success(`Imported ${imported.length} businesses`);
      setShowImportModal(false);
    } catch (error: any) {
      toast.error(`Import failed: ${error.message}`);
      throw error;
    }
  };

  const handleBulkStageUpdate = async (newStage: PipelineStage) => {
    const ids = Array.from(selectedIds);
    try {
      await businessQueries.bulkUpdateStage(ids, newStage);
      
      setBusinesses(businesses.map(b =>
        selectedIds.has(b.id) ? { ...b, pipeline_stage: newStage } : b
      ));
      
      setSelectedIds(new Set());
      toast.success(`Moved ${ids.length} businesses to ${PIPELINE_STAGES.find(s => s.stage === newStage)?.label}`);
    } catch (error: any) {
      toast.error(`Update failed: ${error.message}`);
    }
  };

  const handleBulkEmailStatusUpdate = async (status: EmailStatus) => {
    const ids = Array.from(selectedIds);
    try {
      await businessQueries.bulkUpdateEmailStatus(ids, status);
      
      setBusinesses(businesses.map(b =>
        selectedIds.has(b.id) ? { ...b, email_status: status } : b
      ));
      
      setSelectedIds(new Set());
      toast.success(`Updated ${ids.length} businesses`);
    } catch (error: any) {
      toast.error(`Update failed: ${error.message}`);
    }
  };

  const toggleSelection = (id: string) => {
    const newSelection = new Set(selectedIds);
    if (newSelection.has(id)) {
      newSelection.delete(id);
    } else {
      newSelection.add(id);
    }
    setSelectedIds(newSelection);
  };

  const toggleSelectAll = () => {
    if (selectedIds.size === filteredBusinesses.length) {
      setSelectedIds(new Set());
    } else {
      setSelectedIds(new Set(filteredBusinesses.map(b => b.id)));
    }
  };

  const selectedBusinesses = businesses.filter(b => selectedIds.has(b.id));

  if (loading) {
    return (
      <div className="flex items-center justify-center h-96">
        <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
      </div>
    );
  }

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold text-gray-900">Businesses</h1>
          <p className="text-gray-600 mt-1">
            {filteredBusinesses.length} businesses • {selectedIds.size} selected
          </p>
        </div>
        <Button onClick={() => setShowImportModal(true)}>
          Import CSV
        </Button>
      </div>

      <FilterBar
        filters={filters}
        onFiltersChange={setFilters}
        onClear={() => setFilters(EMPTY_FILTERS)}
      />

      {selectedIds.size > 0 && (
        <div className="bg-blue-50 border border-blue-200 rounded-xl p-4 flex items-center gap-4">
          <span className="text-sm font-medium text-blue-900">
            {selectedIds.size} selected
          </span>
          <div className="flex gap-2">
            <select
              onChange={(e) => handleBulkStageUpdate(e.target.value as PipelineStage)}
              className="px-3 py-1.5 rounded-lg border border-blue-200 text-sm"
              defaultValue=""
            >
              <option value="" disabled>Move to stage...</option>
              {PIPELINE_STAGES.map(stage => (
                <option key={stage.stage} value={stage.stage}>
                  {stage.label}
                </option>
              ))}
            </select>
            
            <select
              onChange={(e) => handleBulkEmailStatusUpdate(e.target.value as EmailStatus)}
              className="px-3 py-1.5 rounded-lg border border-blue-200 text-sm"
              defaultValue=""
            >
              <option value="" disabled>Update email status...</option>
              <option value="not_sent">Not Sent</option>
              <option value="sent">Sent</option>
              <option value="opened">Opened</option>
              <option value="clicked">Clicked</option>
              <option value="replied">Replied</option>
              <option value="bounced">Bounced</option>
            </select>

            <ExportButton
              businesses={selectedBusinesses}
              campaignName="export"
            />

            <Button variant="ghost" size="sm" onClick={() => setSelectedIds(new Set())}>
              Clear Selection
            </Button>
          </div>
        </div>
      )}

      <div className="bg-white rounded-xl border border-black/10 overflow-hidden">
        <table className="min-w-full divide-y divide-black/10">
          <thead className="bg-gray-50">
            <tr>
              <th className="px-4 py-3 text-left">
                <input
                  type="checkbox"
                  checked={selectedIds.size === filteredBusinesses.length && filteredBusinesses.length > 0}
                  onChange={toggleSelectAll}
                  className="rounded border-gray-300 text-blue-600"
                />
              </th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Business</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">City</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Reviews</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Pricing</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Project Value</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Stage</th>
              <th className="px-4 py-3 text-left text-xs font-medium text-gray-700 uppercase">Email</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-black/10">
            {filteredBusinesses.map(business => {
              const stageConfig = PIPELINE_STAGES.find(s => s.stage === business.pipeline_stage);
              
              return (
                <tr key={business.id} className="hover:bg-gray-50">
                  <td className="px-4 py-3">
                    <input
                      type="checkbox"
                      checked={selectedIds.has(business.id)}
                      onChange={() => toggleSelection(business.id)}
                      className="rounded border-gray-300 text-blue-600"
                    />
                  </td>
                  <td className="px-4 py-3">
                    <div className="font-medium text-gray-900">{business.business_name}</div>
                    <div className="text-sm text-gray-500">{business.email}</div>
                  </td>
                  <td className="px-4 py-3 text-sm text-gray-900">{business.city}</td>
                  <td className="px-4 py-3">
                    <div className="text-sm">
                      <span className="font-medium text-gray-900">{business.negative_reviews_count}</span>
                      <span className="text-gray-500"> negative</span>
                    </div>
                    <div className="text-xs text-gray-500">{business.media_reviews_count} total</div>
                  </td>
                  <td className="px-4 py-3">
                    <span className={`inline-flex px-2 py-1 rounded-lg text-xs font-medium ${
                      business.pricing_tier === 'gold' ? 'bg-amber-100 text-amber-800' :
                      business.pricing_tier === 'silver' ? 'bg-gray-100 text-gray-800' :
                      'bg-yellow-100 text-yellow-800'
                    }`}>
                      {business.pricing_tier} (${business.price_per_review}/review)
                    </span>
                  </td>
                  <td className="px-4 py-3 font-medium text-gray-900">
                    ${business.total_project_value.toLocaleString()}
                  </td>
                  <td className="px-4 py-3">
                    <span className="text-sm" style={{ color: stageConfig?.color }}>
                      {stageConfig?.label}
                    </span>
                  </td>
                  <td className="px-4 py-3">
                    <span className="text-sm text-gray-600 capitalize">
                      {business.email_status.replace('_', ' ')}
                    </span>
                  </td>
                </tr>
              );
            })}
          </tbody>
        </table>

        {filteredBusinesses.length === 0 && (
          <div className="text-center py-12">
            <p className="text-gray-500">No businesses found. Import a CSV to get started.</p>
          </div>
        )}
      </div>

      <ImportModal
        isOpen={showImportModal}
        onClose={() => setShowImportModal(false)}
        onImport={handleImport}
      />
    </div>
  );
}
```

---

VALIDATION CHECKLIST:
- .env.local created with actual credentials (Supabase + Firecrawl)
- Inter font from next/font/google
- Sonner toaster component included (bottom-right, richColors)
- Clay design system applied (#F8F6F3 background, border-black/10)
- Navigation with active state underline animation
- Bulk actions toolbar (stage update, email status, export)
- Client-side filtering (search, stages, email statuses, pricing tiers)
- Select all checkbox (reflects filteredBusinesses state)
- Color-coded pricing tier badges (bronze=yellow, silver=gray, gold=amber)
- Color-coded pipeline stages (from PIPELINE_STAGES config)
- Empty state message
- Loading state spinner
- Proper TypeScript typing throughout
- No 'any' types
- All async operations with error handling

OUTPUT: Complete, production-ready app with NO placeholders. Every feature fully implemented and functional. Ready for npm run dev.

Run if everything is 100% perfect: .claude\commands\refactor-code.md -and afterwards- .claude/commands/commit.md and use this command after